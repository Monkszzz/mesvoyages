<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nouveau message de contact</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h2 {
            color: #007bff;
            border-bottom: 2px solid #007bff;
            padding-bottom: 5px;
        }
        p {
            font-size: 16px;
            line-height: 1.5;
        }
        .footer {
            margin-top: 20px;
            font-size: 14px;
            color: #777;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Nouveau message de contact</h2>
        <p><strong>Nom :</strong> {{ contact.nom }}</p>
        <p><strong>Email :</strong> {{ contact.email }}</p>
        <p><strong>Message :</strong></p>
        <p>{{ contact.message|nl2br }}</p>
        <div class="footer">
            <p>Ce message a été envoyé depuis le site MesVoyages.</p>
        </div>
    </div>
</body>
</html>
